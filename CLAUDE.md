# CLAUDE.md

Compatibility shim for tools and workflows that look for `CLAUDE.md`.

Use `AGENTS.md` as the primary project instruction file.

The same rules apply here:

- architecture docs are source of truth
- `roadmap.md` must be maintained
- TDD is the default workflow
- public functions must be reflected in `docs/function-catalog.md`
- agent-facing work should follow `docs/agent-integration-plan.md`
